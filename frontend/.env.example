# API Backend URL
# Production: Use your actual API domain (e.g., https://api.yourdomain.com)
# Development: Use localhost with HTTP (FrankenPHP on port 80)
NEXT_PUBLIC_API_URL=http://localhost

# Mercure Hub URL
# Production: Use your actual Mercure hub URL (e.g., https://mercure.yourdomain.com/.well-known/mercure)
# Development: Use localhost with HTTP (same as API)
NEXT_PUBLIC_MERCURE_HUB_URL=http://localhost/.well-known/mercure

# Node.js Development Settings
# Set to 0 to allow self-signed certificates in development
# IMPORTANT: Only use in development, never in production!
NODE_TLS_REJECT_UNAUTHORIZED=0
